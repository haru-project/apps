cmake_minimum_required(VERSION 3.16)
project(haru_recording_bringup)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)
ament_python_install_package(${PROJECT_NAME})
install(DIRECTORY launch DESTINATION share/${PROJECT_NAME})
install(PROGRAMS scripts/recording_dds_profile.py scripts/recording_load_test.py scripts/import_recording.py scripts/test_publisher.py scripts/routing_test_publisher.py DESTINATION lib/${PROJECT_NAME})
if(BUILD_TESTING)
  find_package(ament_cmake_pytest REQUIRED)
  ament_add_pytest_test(test_peer_diagnostics test/test_peer_diagnostics.py TIMEOUT 30)
  ament_add_pytest_test(test_dds_profile test/test_dds_profile.py TIMEOUT 30)
  ament_add_pytest_test(test_replay_acceptance test/test_replay_acceptance.py TIMEOUT 30)
endif()
ament_package()
